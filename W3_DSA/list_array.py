#array's are pretty easy to handle in the python 
#because list is array in the python 

lst = [] #<--empty array or list 

lst = [1,2,3,4] #<--array with element

lst = [1,True, 'hello', 2.22224] #array with different element 

lst2 = lst.sort() #array's with built in methodes to sort 

